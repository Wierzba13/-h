package interfaces;

public class Main {
    public static void main(String[] args) {
/*
    0. Interface
         * moga zawierac STALE, nie zmienne
         * metody instancji sa domyslnie public abstract
         * zawieraja cialo metody tylko wtedy gdy sa zarowno default LUB private LUB static

         Definicja interface'u
         public interface <InterfaceName> {
            public static final String male = "MALE"; // its const
            void sayHello();
            public default boolean isAdult(int age) {
                return age >= 18;
            }
         }
         Interface'y sa typem (sa odpowiednim typem referencyjnym)
    1. Klasa moze implementowac wiele interfejsow (rozszerzac moze tylko 1 klase abstrakcyjna)
    2. Interface moze rozszerzac inny interface
    public interface X extends Y {
        // interface body...
    }
    W takim przypadku klasa implementujaca interface X musi rowniez zaimplementowac metody z interface'u Y

    3. Functional Interface
        Interfejs funkcyjny jest interfejsem, który posiada tylko jedną metodę abstrakcyjną.
*/




    }
}
